#  @configure_input@
# package -speci substitution variables

package = @PACKAGE_NAME@
version = @PACKAGE_VERSION@ 
tarname =@PACKAGE_TARNAME@
distdir = $(tarname)-$(version)

# prefix -speci substituion variable
prefix= @prefix@
exec_prefix =@exec_prefix@
bindir = @bindir@

# VPATH staff
srcdir = @srcdir@
VPATH =  @srcdir@

#Tool specif substitutin var
CC	=  @CC@
DEFS    = @DEFS@
CFLAGS  = @CFLAGS@
CPPFLAGS =@CPPFLAGS@
LIBS	 = @LIBS@
INSTALL  =@INSTALL@
INSTALL_DATA  =@INSTALL_DATA@
INSTALL_PROGRAM= @INSTALL_PROGRAM@
INSTALL_SCRIPT = @INSTALL_SCRIPT@


all: jupiter


jupiter: main.c
#	gcc -g -O0 -o $@ $(srcdir)/main.c
	$(CC) -I. -I$(srcdir) -I.. $(DEF) $(CPPFLAGS) $(CFLAGS) -o $@ main.c  $(LIBS)

#	gcc -g -O0 -o $@ $(srcdir)/main.c

check: all
	./jupiter | grep "Hello from .*jupiter"
	@echo "*** all Test passed ***"
clean:
	-rm jupiter


#3rd way of install
	$(INSTALL) -d $(DESTDIR)$(bindir)/jupiter
	$(INSTTTALL_PROGRAM) -m 0755 jupiter $(DESTDIR)$(bindir)/jupiter

# 1st way of install

install:
#	install -d $(prifix)/bin
#	install -m 0755 jupiter $(prefix)/bin
# 2nd way of  install
	
#	cp jupiter /usr/bin
#	chown root:root /usr/bin/jupiter
#	chmod +x /usr/bin/jupiter
Makefile: Makefile.in ../config.status
	./config.status src/$@
../config.status: ../configure
	cd .. && ./config.status --recheck

.PHONE: all clean check


